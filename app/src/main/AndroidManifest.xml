<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.mpr">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
        <activity android:name=".controlers.Categoria">
        </activity>
        <activity android:name=".controlers.Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".controlers.Evento"/>
        <activity android:name=".controlers.Tematica"/>
        <activity android:name=".controlers.ProductoActivity"/>
        <activity android:name=".controlers.ProductoDetalle"/>
        <activity android:name=".controlers.Favorito"/>
        <activity android:name=".controlers.DetalleFavorito"/>
    </application>

</manifest>